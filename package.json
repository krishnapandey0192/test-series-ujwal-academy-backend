{
  "name": "ttms-backend",
  "version": "1.0.0",
  "description": "Test Management System Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "kill-port": "echo \"To kill process on port: taskkill /F /PID (netstat -ano | findstr :PORT)\"",
    "check-ports": "netstat -ano | findstr :8000",
    "clean-uploads": "rimraf uploads/*",
    "setup": "npm install && mkdir -p uploads"
  },
  "keywords": [
    "test",
    "management",
    "system",
    "api",
    "mongodb",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.1",
    "multer": "^2.0.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "rimraf": "^3.0.2"
  }
}
